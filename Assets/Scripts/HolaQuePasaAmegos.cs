using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class HolaQuePasaAmegos : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        Debug.Log("guthab");
    }

    // Update is called once per frame
    void Update()
    {
        Debug.Log("Hola Que pasa amegos soy Antonio P Valls");
    }
}
